[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
time = { version = "0.3.41", features = ["formatting", "parsing", "macros", "serde"] }

[dev-dependencies]
rstest = "0.25.0"
serde_json = "1.0.140"
