{
  "name": "@aura-historia/deploy-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=26 <27" },
  "bin": { "aura-deploy": "dist/src/cli/main.js" },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "schemas": "npm run build && node dist/src/contracts/export-schemas.js",
    "validate-catalog": "npm run build && node dist/src/cli/main.js release validate-catalog"
  },
  "dependencies": { "zod": "4.1.12" },
  "devDependencies": { "@types/node": "26.5.0", "typescript": "6.0.3" }
}
